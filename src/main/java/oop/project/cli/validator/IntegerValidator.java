/*
package oop.project.cli.validator;

import oop.project.cli.exception.ValidateException;

public class IntegerValidator implements Validator {
    @Override
    public void validate(Object val) throws ValidateException {
        try {
            val instanceof Integer ? ((Integer) val) : ValidateException;
            final Integer i = !(val instanceof Integer) ? null : ((Integer) val);
        } catch (ValidateException e) {
            throw new ValidateException("Invalid integer.");
        }

    }
}
*/
